lsblk		#This command lists all block devices in the system as trees. If a device is mounted somewhere, its mounting point is also shown.

echo 1>/sys/class/block/sdd/device/rescan
OR
echo 1>/sys/class/scsi_device/X:X:X:X/device/block/device/rescan    # Re-scan disk using below command :